# Vue 3 — практика, чек-лист по блокам

> Отмечай `[x]` по мере прохождения. Список честно отражает, что уже сделано на текущий
> момент (30 июля) — включая два места, которые изначально были пропущены и добавлены
> задним числом: 1.5 и 5.2/5.3.

---

## Блок 1 — синтаксис шаблона, без реактивности

- [x] 1.1 — интерполяция (`username`, `posts.length`)
- [x] 1.2 — статичный `:bind` (`docsLink`, `linkText`)
- [x] 1.3 — `@click` и обработчик события (`sayHello`)
- [x] 1.4 — `v-if` на статичном булевом значении (`isLoggedIn`)
- [ ] 1.5 — сравнение `v-if` и `v-show` бок о бок на одном элементе *(было пропущено, ещё не сделано)*

## Блок 2 — data() и methods

- [x] 2.1 — новое поле + метод, который его меняет (`counter`, `incrementCounter`)
- [x] 2.2 — ручная реализация двусторонней связи через `:value`/`@input`, затем замена на `v-model` (`nickname`)

## Блок 3 — computed и watch

- [x] 3.1 — первое computed-свойство (`doubleCounter`)
- [x] 3.2 — computed для сортировки списка (`sortedPosts`)
- [x] 3.3 — watch за полем формы (`nickname` → консоль)
- [x] доп. — практика на `v-for` (редактируемый список, независимый список "покупки" с нуля)

## Блок 4 — v-for и списки

- [x] 4.1 — добавление элемента в реактивный массив (`addToShoppingList`, `addFavoriteMovie`)
- [x] 4.2 — удаление элемента по id через `filter` (`deleteList`, `deleteMovie`)
- [x] `:key` в `v-for` — разобрано и применено во всех списках

## Блок 5 — компоненты и props

- [x] 5.1 — вынос куска разметки в новый компонент (`MovieFilms.vue`)
- [x] 5.1 (повтор) — то же самое самостоятельно, второй компонент (`ShoppingList.vue`)
- [ ] 5.2 — передать проп вниз, из родителя в новый дочерний компонент *(было пропущено, ещё не сделано)*
- [ ] 5.3 — добавить валидацию типа пропа (`type`, `required`) *(ещё не сделано)*

## Блок 6 — $emit (данные вверх)

- [ ] 6.1 — починить `PostForm.vue` через `$emit` по-настоящему *(код выдан, установка/проверка в браузере — на твоей стороне, ещё не подтверждена)*
- [ ] 6.2 — кнопка "Удалить" в `PostList.vue`, эмитящая событие наверх в `App.vue`
- [ ] 6.3 — переименовать/почистить: убрать мёртвые `createPost`/`inputTitle`/`inputBody` из `App.vue` после перехода на `addPost`

## Блок 7 — слоты

- [ ] 7.1 — обернуть карточку поста через `<slot>`
- [ ] 7.2 — именованные слоты (`<slot name="...">`)

## Блок 8 — хуки жизненного цикла

- [ ] 8.1 — лог в `created`/`mounted`
- [ ] 8.2 — `mounted` для автофокуса на поле ввода при открытии формы

## Блок 9 — мини-проект, собираем всё вместе

- [ ] 9.1 — поиск по постам (`computed` + `v-model`)
- [ ] 9.2 — кнопка "Избранное" на посте
- [ ] 9.3 — счётчик количества постов
- [ ] 9.4 — финальная сборка — рабочее приложение со всеми фичами блоков 1–8

## Блок 10 — переход на Composition API (после блока 9)

- [ ] Переписать финальный `App.vue` на `setup()`/`ref()`, по одному полю за раз
- [ ] Конкретные подпункты появятся, когда дойдём — пока не расписываю заранее, чтобы не плодить те же самые накладки
